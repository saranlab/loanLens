-r requirements.txt

# Test suite and the tooling that executes the notebook with its outputs.
pytest>=8.0
nbformat>=5.9
nbclient>=0.9

# tests/test_api.py drives the service with fastapi's TestClient, which needs httpx.
fastapi>=0.115
httpx>=0.27
